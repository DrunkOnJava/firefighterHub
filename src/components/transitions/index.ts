/**
 * Transitions Library - Index
 * 
 * Central export point for all transition and state components.
 * 
 * Usage:
 * ```tsx
 * import { PageTransition, Collapsible, EmptyState } from './components/transitions';
 * ```
 */

// Page Transitions
export { PageTransition, ModalTransition, SlideSheet } from './PageTransition';

// Collapsible Components
export { Collapsible, Accordion } from './Collapsible';

// Empty States
export { EmptyState, EmptyStateCard } from './EmptyState';
