Update calendar grid to use smaller gaps and better sizing:

calendar: {
    dayGrid: [
      'grid',
      'grid-cols-7',
      'auto-rows-fr',                 
      'gap-1.5 md:gap-2 lg:gap-2',   // Reduced gap
      'w-full',
      'h-full',
    ].join(' '),
    
    weekdayHeader: [
      'grid',
      'grid-cols-7',
      'gap-1.5 md:gap-2 lg:gap-2',   // Reduced gap  
      'w-full',
    ].join(' '),
}
